<h1>All Tasks</h1>

<ul><% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task %> <%= link_to "Delete", task_path(task),
      method: :delete %></li>
  <% end %>
</ul>

<%= link_to "Create new task", new_path %>
